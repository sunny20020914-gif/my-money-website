# My Money Web - データ構造およびGAS運用ロジック仕様書

本ドキュメントは、My Money Webのフロントエンド（Next.js）が参照しているバックエンド（Google スプレッドシート）のデータスキーマ、およびGoogle Apps Script（GAS）によるデータ自動生成ロジックをまとめたものです。
今後のコード修正や自動化機能の実装時は、この仕様と運用上の課題を前提として設計を行ってください。

## 1. スプレッドシートのデータ構造（スキーマ）

本システムは、主に「ランキング一覧用データ」と「企業詳細ページ用データ」の2つのシートを連携させて構築されています。

### 1-1. ランキング用シート（例: 月額額面ランキング、年俸ランキング）
各企業の基礎データとランキング表示用の指標を管理します。

| 列 | 項目名 | 説明・GASでの参照 |
| :--- | :--- | :--- |
| **B列** | 企業名 | 企業の正式名称（Geminiのプロンプト生成に使用） |
| **D列** | 年俸 | 想定年収データ |
| **E列** | 基本月額 | 初任給（月額）データ |
| **F列** | 従業員数 | 企業規模 |
| **G列** | 設立年 | 設立年度 |
| **H列** | 説明文 | ランキング一覧に表示される短い企業説明 |
| **J列** | ドメイン | 公式サイトURLやドメイン（ロゴ自動取得のキー） |
| **K列** | ロゴURL | GASによって自動取得されたロゴ画像のURL |
| **L列** | 識別番号 (ID) | **【最重要】** 詳細ページデータと紐づけるユニークID |

### 1-2. 企業詳細データ用シート（companies / 企業詳細ページ作業用）
詳細ページ（`/companies/[id]`）で表示する長文テキストコンテンツを管理します。

| 列 | 項目名 | 説明・GASでの参照 |
| :--- | :--- | :--- |
| **A列** | id | **【最重要】** ランキングシートのL列と完全一致させるユニークID |
| **B列** | long_desc | 企業の概要（約700字・Markdownパース用） |
| **C列** | strength | 企業の強み（約70字・Markdownパース用） |
| **D列** | potential | 企業の将来性（約40字・Markdownパース用） |
| **E列** | salary_details| 給与に関する補足情報 |
| **H列** | 企業名 | 人間が管理するための企業名メモ |

---

## 2. GASによる自動化ロジック

スプレッドシート側では、以下の自動化スクリプト（GAS）が稼働し、データを補完しています。

1. **ロゴ画像の自動取得 (`updateAllLogoUrls`)**
   - ランキングシートのJ列（ドメイン）を読み取り、Google Favicon API（またはClearbit API）を叩いてロゴ画像を生成。
   - K列が空欄の場合のみ処理を行い、取得した画像URLをK列に書き込みます。
2. **企業詳細テキストの一括生成 (`generateCompanyInfo`)**
   - ランキングシート（B列:企業名, L列:ID）と詳細シート（A列:ID）を突合。
   - 共通のIDが存在し、かつ詳細シートのB/C/D列が「未入力」の行を対象に、Gemini API（gemini-2.5-flash）へリクエストを送信。
   - 概要・強み・将来性のテキストをバッチ処理で生成し、詳細シートへ自動書き込みを行います。

---

## 3. 今後の運用上の課題と、AI（Claude）への要請

フロントエンド（Next.js）側の管理や機能拡張を行う際は、以下の運用課題を自動化・システム側でカバーできるようにコードを提案してください。

### 課題①：ランキング掲載企業と詳細ページの不一致防止（データの完全同期）
- **現状の課題:** 新しい企業をランキング（スプシ）に追加した際、詳細シート側に同じID（A列）の行を作らないと詳細ページが404になります。
- **要請:** フロントエンド側で、万が一詳細データ（長文テキスト）が存在しなくてもページがクラッシュせず、基礎データ（給与や従業員数）だけで詳細ページがフォールバック表示されるような堅牢なエラーハンドリングを実装してください。

### 課題②：企業のアイコン画像（ロゴ）の安定表示
- **現状の課題:** GASでFavicon等から自動取得した画像は、画質が粗かったりリンク切れを起こすリスクがあります。
- **要請:** `next/image` コンポーネントを使用する際、K列のロゴURLがリンク切れだった場合にデフォルトのプレースホルダー画像へ美しくフォールバックするロジックを必ず組み込んでください。

### 課題③：金額や情報の更新時のパフォーマンス
- **現状の課題:** スプシ側で給与額や企業テキストを更新した際、サイト側に即座に反映される必要があります。
- **要請:** Next.jsの `revalidate`（ISR）やデータフェッチのキャッシュ戦略において、スプシの更新頻度に合わせた最適なパージ（再検証）間隔を設定し、パフォーマンスとリアルタイム性を両立させてください。